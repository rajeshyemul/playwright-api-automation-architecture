/**
 * AuthModels re-exports types derived from AuthContract Zod schemas.
 * See models/UserModels.ts for the architectural note on this pattern.
 */
export type { LoginRequest, LoginResponse } from '../contracts/AuthContract';
